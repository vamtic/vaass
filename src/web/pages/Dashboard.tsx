import Head from '../components/Head.tsx';
import type { Upload } from '../../types/Upload.ts';

export default () => {
	return (
		<html>
			<Head title='Dashboard' />
			<body>dashboard</body>
		</html>
	);
};
